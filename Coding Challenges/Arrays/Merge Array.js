/*Given two sorted arrays, merge them into a single array Quesions to ask: Is the array sorted? Will it have only integers? Can it have duplicates? Can you allocate another array for the result?*\

let a=[1,2,3];
let b=[4,5,6];
const mergedArray=
   a.concat(b);
console.log(mergedArray);